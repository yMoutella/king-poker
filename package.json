{
  "name": "king-poker",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.3"
  },
  "packageManager": "yarn@1.22.22",
  "dependencies": {
    "global": "^4.4.0"
  }
}
